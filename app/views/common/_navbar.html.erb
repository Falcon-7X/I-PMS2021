<div class="topnav">
  <a class="active" href="/">I-PMS</a>
  <a href="#news">News</a>
  <a href="#contact">Contact</a>

  <div class="topnav-right">
    <% if !current_user %>
      <a> <%= link_to("Signup", new_user_registration_path) %> </a>
      <a> <%= link_to("Login", new_user_session_path) %> </a>
    <% else %>
      <a class="active-email"> <%= current_user.email %> </a>
      <a> <%= link_to("Logout", destroy_user_session_path, method: :delete) %> </a>
    <% end %>
  </div>
</div>
